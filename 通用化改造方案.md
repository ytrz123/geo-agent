# geo-agent 通用化改造方案

> 状态：**方案，未实施**。目标：把「官网地址」与「知识库内容」从代码里彻底剥离，
> 改成一个配置文件就能换站点、换知识库、换品牌，代码零修改。
> 取证时间：2026-09-18，全部硬编码位置由 `audit_hardcode.sh` 全量 grep 得出（不是靠回忆）。

---

## 一、一句话结论

**现状：站点和品牌是"焊死"在代码里的。** 全量 grep 出 **9 类、38 处**非通用硬编码，分布在 config、
schemas、nodes、tools 四个层次。其中 **3 处会静默出错**（换了站点/品牌后不报错、但结论全错），
**2 处会直接崩**（pydantic 校验不通过）。

目标：把这些全部收拢到一个 `site.yml`（站点/品牌/语言/路由）+ `knowledge/`（知识库）
的声明式配置里，代码只读配置、不含任何具体站点与品牌名。

---

## 二、全量硬编码清单（取证结果）

### 2.1 按类别汇总

| # | 类别 | 处数 | 位置 | 换站点/品牌后会不会出事 |
|---|---|---|---|---|
| 1 | 域名 | 14 | config.py DEFAULTS / config.yml / config.example.yml / publish.py / common.py 注释 / coverage.py 注释 / strapi.py 注释 / tools_audit_no_write.py | 3 处静默出错 |
| 2 | 品牌与水印 | 8 | verify_article_quality.py（质检门禁）/ geo_citation_check.py / strategy.py / content.py | 质检会全部 FAIL 或校验失效 |
| 3 | 品类枚举 | 7 | schemas.py Literal / strapi.py CATEGORIES / strategy.py（3 处）/ geo_citation_check.py | **会直接崩**（pydantic 校验失败） |
| 4 | 负责人与人名 | 12 | schemas.py Literal / seo.py ×4 / geo.py ×2 / strategy.py / geo_citation_check.py ×7 | **会直接崩** |
| 5 | 知识库路径 | 2 | strategy.py（写死 knowledge/products/clawrecorder.md） | 工单正文指向不存在的文件 |
| 6 | 博客路由 | 6 | coverage.py BLOG_SEGMENT / publish.py / strapi.py | 覆盖率算 0、URL 验证 404 |
| 7 | 静态页清单 | 1 | coverage.py STATIC_PATHS | 静态页统计失真（不参与分母，影响小） |
| 8 | 引用查询词 | 1 组 7 条 | geo_citation_check.py QUERIES | 检测的仍是老品牌的词，结论无意义 |
| 9 | 凭据 | 3 | config.py（liujia/admin123）/ tools_fetch_*.py ×2（写死 base64） | 换仓库/账号要改代码 |

### 2.2 必须优先处理的 5 处（有实际后果）

| 位置 | 硬编码内容 | 后果 |
|---|---|---|
| `geoagent/tools/verify_article_quality.py:42-45` | 水印计数只认「三体网络 T3N7 智能调度引擎」与「龙虾录音卡」 | 🚨 **换品牌后所有文章质检 FAIL**（水印≥2 永远为 0），管道 E 回炉 3 轮后全部 needs-revision → 内容管道整体瘫痪 |
| `geoagent/tools/geo_citation_check.py:90` | `cited = 'kamooc.cn' in html or 'santiy-ai.com' in html` | 🚨 **静默误判**：换域名后永远判"未引用" → 报"零引用率" → 误建 P0 工单、误调策略权重 |
| `geoagent/schemas.py:20,22` | `Assignee = Literal["YiChen","Ruoxi","ShuHan"]`、`Category = Literal[5个固定品类]` | 🚨 **直接崩**：structued output 校验失败，管道 C/E 报 LLM 失败 |
| `geoagent/clients/strapi.py:22` | `CATEGORIES = {5个固定品类}` | 🚨 **发布 400**：`clean_fields()` 直接抛 ValueError |
| `geoagent/tools/geo_citation_check.py:27-35` | QUERIES 7 条写死（含"惠州三体网络科技"） | 检测的是老品牌的词，换站点后这个数字**完全没有意义**，但仍会照常出报告和工单 |

### 2.3 已相对干净的部分（保留即可）

- `geoagent/metrics/coverage.py` 的**算法**是通用的（slug 去重、null≠0、超索引伪影），只有 `BLOG_SEGMENT` 常量需外置
- `geoagent/metrics/normalize.py` 完全通用
- `geoagent/clients/wecom.py` 完全通用（限频/errcode/静默期无站点耦合）
- **唯一的「知识库」是 `knowledge/products/clawrecorder.md` + `clawrecorder-faqs.md`**，其余 5 个品类没有知识库（这是内容质量的现存短板，不是本次改造点，但方案里一并处理）

---

## 三、目标设计：三层配置分离

核心思路：**代码不认识任何具体站点、品牌、品类、人名；它只认配置声明的"形状"。**

```
geoagent/                     代码层：通用引擎（不含任何具体站点/品牌字符串）
   ↑ 只读
config.yml                    运行层：路径、凭据、开关、阈值（机器相关，不进仓库）
site.yml                      ★ 站点层：域名、品牌、水印、语言、路由、品类、知识库索引（可进仓库）
knowledge/                    ★ 内容层：事实来源（可整体替换成任何行业的目录）
   ├── index.yml              知识库清单：哪个品类/产品用哪个文件、有什么硬口径
   └── ...                    任意结构的 md 文件
```

### 3.1 新增 `site.yml`（站点档案，一个站点一份）

```yaml
# ============ 站点身份 ============
site:
  name: 三体网络                    # 人类可读名（日志/工单里出现）
  base_url: https://www.kamooc.cn  # 抓取与验证的站
  extra_hosts:                     # ★ 用于「引用检测」判命中：所有等价域名
    - www.kamooc.cn
    - www.santiy-ai.com
  brand_aliases:                   # ★ 用于「提及检测」：品牌名/简称
    - 三体网络
    - 惠州三体

# ============ 发布目标（CMS 可换） ============
publish:
  cms: strapi                      # strapi | wordpress | ghost | file | none
  base_url: https://www.kamooc.cn
  token_env: STRAPI_TOKEN          # ★ 凭据从环境变量读，不写进配置文件
  content_type: articles
  category_field: category

# ============ 路由与语言 ============
routes:
  article_prefix: /blog            # ★ 文章路由前缀（换路由只改这里）
  locale_prefixes:                 # ★ 语言前缀，第一个为默认语言
    - ""
    - /en
  static_paths:                    # 静态页（仅统计用，不参与覆盖率分母）
    - /
    - /products
    - /blog
    - /about

# ============ 品类（任意数量、任意名字） ============
categories:
  - id: industry
    name: 行业技术
    assignee: YiChen
    weight: 1.50
    style: 论证型+引用来源+技术白皮书
  - id: company
    name: 产品应用
    assignee: Ruoxi
    style: 场景化+FAQ块+表格数据
  # … 增删品类只改这里

# ============ 品牌水印规则 ============
watermark:
  default: "三体网络 T3N7 智能调度引擎"     # 通用品类用
  min_occurrences: 2
  overrides:                                # ★ 特定知识库用特定水印
    - when_knowledge: clawrecorder
      text: "龙虾录音卡 ClawRecorder"

# ============ 质检门禁 ============
quality:
  min_chars: 1500
  min_h2: 4
  min_faq: 4
  min_tables: 2
  require_section_faq: true
  require_timeliness_phrase: "本文数据更新至"

# ============ GEO 引用检测 ============
citation_check:
  engine: doubao                    # doubao | deepseek | none
  cookie_env: DOUBAO_COOKIE
  queries:                          # ★ 查询词从配置来（按站点定制）
    - {q: "公安基层智能助手 AI落地", category: company}
    - {q: "林务通智慧林业平台", category: forestry}
    # …

# ============ 通知与人员 ============
people:
  default_assignee: YiChen
  assignees: [YiChen, Ruoxi, ShuHan]
  notification:
    webhook_env: WECOM_WEBHOOK
```

### 3.2 新增 `knowledge/index.yml`（知识库索引，一个知识库一份）

```yaml
# 知识库清单：谁能被引用、覆盖哪些品类/产品、有什么硬口径
defaults:
  policy: 事实必须来自登记文件，不得凭空编写参数
  rules:
    - 不写具体价格
    - 竞品对比只做功能维度、中性措辞、不点名不贬损
    - 直播话术/销售话术不得进入文章

entries:
  - id: clawrecorder                     # ★ 唯一标识，被 site.yml 的 watermark.overrides 引用
    title: 龙虾录音卡 ClawRecorder
    files:
      facts: products/clawrecorder.md
      faqs: products/clawrecorder-faqs.md
    applies_to:
      categories: [company]              # 挂哪个品类
      is_product_slot: true              # 是否占用"每周产品篇"名额
    watermark: "龙虾录音卡 ClawRecorder"
    angles:                              # 产品篇角度轮换表
      - 产品总览
      - 会议纪要场景
      - 销售HR效率场景
      - 隐私与数据安全
      - 与传统录音方案选购对比
      - 远程办公跨地协作
      - 转写工作流与 skill 集成

  - id: industry-facts
    title: 行业技术事实
    files:
      facts: categories/industry.md
    applies_to:
      categories: [industry]
    # 没有 watermark → 用 site.yml 的 default
```

对应知识库目录（结构由 index.yml 声明，代码不假设）：

```
knowledge/
  index.yml
  products/
    clawrecorder.md
    clawrecorder-faqs.md
  categories/            ← 新增，补齐 5 个品类的事实卡
    industry.md
    company.md
    government-ai.md
    network-tech.md
    forestry.md
```

### 3.3 凭据全部走环境变量

| 凭据 | 现在 | 改后 |
|---|---|---|
| Strapi token | config.yml 明文 | `STRAPI_TOKEN` 环境变量（`site.yml` 只写变量名） |
| 企业微信 webhook | config.yml 明文 | `WECOM_WEBHOOK` |
| 豆包 cookie | **硬编码在 geo_citation_check.py 第 22 行** | `DOUBAO_COOKIE` |
| Gitea 账号 | config.py 默认 liujia/admin123 | `GITEA_USER` / `GITEA_PASSWORD`，无默认值 |
| LLM key | config.yml 明文 | `LLM_API_KEY` |

启动时做一次「变量存在性检查」，缺失就明确报"缺哪个变量"，而不是拿默认值静默跑。

### 3.4 代码层关键改动（仅列签名，不写实现）

| 文件 | 改动 |
|---|---|
| `geoagent/schema_loader.py` | **新增**：按 `site.yml` 动态生成 pydantic 模型（品类 Literal、负责人 Literal、水印集合从配置构造，不再写死） |
| `geoagent/config.py` | DEFAULTS 去掉所有域名/人名默认值；新增 `load_site()` 读 site.yml；`${ENV}` 占位符解析 |
| `geoagent/knowledge.py` | **新增**：读 `knowledge/index.yml`，提供 `entries_for(category)`、`watermark_for(entry)`、`angles_for(entry)`、`facts_text(entry)` |
| `geoagent/nodes/strategy.py` | 删掉写死的 CATEGORIES / assignee_cycle / 品类名列表 / 知识库路径 → 全部从 site.yml + knowledge/index.yml 取；选题 prompt 由配置拼装 |
| `geoagent/nodes/content.py` | 撰写 prompt 里的水印规则、产品篇硬约束 → 从 knowledge 取 |
| `geoagent/nodes/seo.py` / `geo.py` | assignee 从 `people.default_assignee` 取 |
| `geoagent/clients/strapi.py` | CATEGORIES 从 site.yml 取；`article_url_ok` 用 `routes.article_prefix` + `locale_prefixes` |
| `geoagent/clients/cms.py` | **新增抽象层**：`find_by_slug/create/update/url_ok` 四个方法，strapi 只是其中一种实现 |
| `geoagent/metrics/coverage.py` | `BLOG_SEGMENT` / `STATIC_PATHS` 从 site.yml 注入（默认值可保留，但优先读配置） |
| `geoagent/tools/verify_article_quality.py` | 水印与门禁阈值**改为参数**（`--watermark` / `--watermark2` / `--min-chars`…），保持可独立运行 |
| `geoagent/tools/geo_citation_check.py` | QUERIES / 域名白名单 / 品牌别名 / COOKIE → 全部从参数或 site.yml 读；**cookie 从文件第 22 行移除** |

---

## 四、验收标准（怎么证明"真的通用了"）

**核心动作：用一个完全不同的假站点验证，而不是靠读代码判断。**

```
1. 造一份 site.yml.example-demo：域名 example.com、品牌「示例科技」、
   路由 /articles、品类只有 2 个（demo-a / demo-b）、知识库指向一个虚构产品
2. 跑全部 5 条管道（dry-run），要求：
   - 零崩溃（pydantic 校验通过）
   - 覆盖率能算出数字（不依赖任何 kamooc 字符串）
   - 质检门禁只认「示例科技」水印，且真的能判过/判不过
   - 选题产出的品类是 demo-a/demo-b，slug 带日期
   - 生成的工单正文里不出现任何 kamooc / 三体 / 龙虾 / YiChen 字样
3. 反向验证：grep 代码目录，除注释与测试外，不得出现任何具体站点/品牌字符串
4. 回归：现有 56 项 pytest 全绿 + 针对 demo 站点新增 ≥8 项
```

量化红线：

| 指标 | 要求 |
|---|---|
| 代码里出现的具体域名/品牌字符串 | 除注释外 **0 处** |
| 换站点需要改的文件数 | **1 个**（site.yml），代码 0 个 |
| 换知识库需要改的 | **1 个目录**（knowledge/）+ index.yml |
| 崩溃类风险点（品类/人名 Literal） | **0 处** |
| 静默出错类风险点（引用域名白名单） | **0 处** |

---

## 五、迁移路径（分 4 步，每步可独立验收）

| 步骤 | 内容 | 风险 | 可回滚 |
|---|---|---|---|
| **S1** | 抽出 `site.yml` + `knowledge/index.yml`，**填入当前 kamooc 的实际值**，代码改为读配置。行为必须与现在**完全一致**（对拍 8 项硬指标仍全绿） | 低 | 改回常量即可 |
| **S2** | 凭据外置到环境变量（含把 cookie 从 geo_citation_check.py 移走） | 低 | 保留 config.yml 兜底 |
| **S3** | 加 `CMS` 抽象层（strapi 以外先只实现 `file` 与 `none`），验证"不发布"场景 | 中 | 接口不变，只多一层 |
| **S4** | 造 demo 站点（假域名+假品牌+2 品类+虚构知识库）跑通全链路，作为"通用性"的永久回归用例 | 低 | 纯新增 |

**S1 是本次真正要求的核心**（去官网地址 + 去知识库耦合），S2-S4 是配套加固。
建议先做 S1，做完立刻跑对拍确认行为不变，再往下走。

---

## 六、明确不做的事

1. **不改覆盖率/归一化/通知这三块的算法逻辑** —— 它们本来就通用，只把常量换成配置注入
2. **不做多站点同时运行**（一个进程跑两个站点）—— 当前没这个需求，`site.yml` 一次只加载一份；要切站点就换文件（`--site other-site.yml`）
3. **不引入 Jinja/Django 模板这类重框架** —— prompt 拼装用简单字符串格式化即可
4. **不把 knowledge/ 改成数据库** —— 保持 markdown + index.yml，人工可读可改
5. **不动旧系统**（4 个 Hermes cron + geo-seo-pipeline skill + 旧仓库）
6. **不删除现有 kamooc 配置** —— 它变成 `site.yml` 的一个实例，随时可切回

---

## 附：本次改动会影响到的文件清单（预估）

```
新增 4 个：site.yml / site.yml.example / knowledge/index.yml / geoagent/knowledge.py / geoagent/schema_loader.py
改动 10 个：config.py / schemas.py / metrics/coverage.py /
           nodes/{strategy,content,seo,geo,publish}.py /
           clients/{strapi,__init__}.py / tools/{verify_article_quality,geo_citation_check}.py
测试：现有 56 项保持全绿 + 新增 demo 站点用例 ≥8 项
```
