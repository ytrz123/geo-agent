# geo-agent 使用说明

> 版本：v0.2（2026-09-18 通用化改造完成）· 状态：影子期（默认只读、默认不发布到官网）
> 目录：`/Users/mac/Documents/geo-agent` —— 独立目录，**不接任何 git remote，不上传代码仓库**
> 原 GEO/SEO 闭环（4 个 Hermes cron + skill）**未被改动，仍在正常运行**

---

## 一、这是什么

把原来「LLM 每次读一遍 SKILL.md 文本再自己决定怎么做」的 GEO/SEO 流程，
换成「代码状态机（LangGraph）+ 只有 3 个判断点调用 LLM」的管道实现。

**并且已经通用化：代码里不含任何具体站点、品牌、品类、人名。**
换网站、换品牌、换知识库，只改配置文件，代码零修改。

```
geoagent/                 代码层：通用引擎（不含任何具体站点字符串）
     ↑ 只读
config.yml                运行层：路径 / 开关 / 凭据 / 客户端地址（机器相关）—— 私有
site.yml                  ★ 站点层：域名、品牌、水印、路由、品类、人员、查询词 —— 私有
knowledge/index.yml       ★ 内容层：知识库清单（事实来源登记）—— 私有

进仓库的只有零真实取值的骨架：config.example.yml / site.example.yml / knowledge/index.example.yml
私有档案的存放位置（三者一致，命令行 → 环境变量 → 项目内 → ~/.config/geoagent/）见 §7.1。
```

五条管道与 LLM 用量：

| 命令 | 管道 | LLM 节点 | 干什么 |
|---|---|---|---|
| `publish` | D 定时发布 | **0** | 扫 status:review → 质检 → md→HTML → 查重 → 发布 → 评论④ → 关闭 → 回写 → push |
| `seo-daily` | A SEO 每日 | **0** | 抓取 ×5 → 算覆盖率 → 规则判定 → 工单去重 → 写 daily → push |
| `geo-weekly` | B GEO 每周 | 1 | 爬虫统计 → 引用检测 → 建议生成 → 工单去重 → 写 weekly |
| `strategy` | C 策略自进化 | 2 | 读 4 周窗口 → 规则+封顶 → 周报叙述 → 选题 → 批次守卫 → 建工单 |
| `content` | E 内容生成 | 1 + 回炉环 | 取工单 → 撰写 → 质检 → FAIL 回炉（≤3 轮）→ 评论③ → push |

---

## 二、快速上手

```bash
cd /Users/mac/Documents/geo-agent

.venv/bin/python cli.py list              # 列出全部管道
.venv/bin/python cli.py check             # ★ 配置自检（站点/知识库/模型，不跑管道）
.venv/bin/python cli.py seo-daily         # 管道 A（只读影子跑，约 3 秒）
.venv/bin/python cli.py publish           # 管道 D
.venv/bin/python cli.py geo-weekly        # 管道 B（默认不跑浏览器）
.venv/bin/python cli.py strategy          # 管道 C（真调 2 次 LLM，约 3-5 分钟）
.venv/bin/python cli.py content --no-llm  # 管道 E 结构验证（不花 token）

.venv/bin/python -m pytest tests -q       # 回归测试（63 项，约 20 秒）

# ★ 换一份站点档案跑同一个管道
.venv/bin/python cli.py seo-daily --site site.example.yml
```

| 开关 | 作用 |
|---|---|
| （不加） | 默认 **dry-run**：只读，零远端写入，产物落 `var/out/` |
| `--apply` | 真写生产。**还需** config.yml 的 `shadow.read_only: false` |
| `--config FILE` | 指定运行配置（默认 `./config.yml` → `~/.config/geoagent/config.yml`） |
| `--site FILE` | 指定站点档案（默认 `./site.yml` → `~/.config/geoagent/site.yml`） |
| `--knowledge FILE` | 指定知识库索引（默认 `./knowledge/index.yml` → `~/.config/geoagent/knowledge/index.yml`） |
| `--notify` | 允许真发企业微信群消息（独立于写权限，影子期也能发） |
| `--no-llm` | LLM 节点置为不可用，只验证结构，不花 token |
| `--with-citation` | 管道 B 真跑引用检测（Playwright + Chrome，2-3 分钟） |
| `--no-checkpoint` | 不用 SqliteSaver 断点续跑 |

---

## 三、换网站怎么做（★ 核心：只改配置，不改代码）

### 3.1 三步搞定

```bash
mkdir -p ~/.config/geoagent/knowledge/products
cp site.example.yml   ~/.config/geoagent/site.yml               # 1. 拿模板（放仓库外）
vim ~/.config/geoagent/site.yml                                 # 2. 改站点身份 + 品类
cp knowledge/index.example.yml ~/.config/geoagent/knowledge/index.yml
vim ~/.config/geoagent/knowledge/index.yml                      # 3. 改知识库清单
```

**代码一行都不用动。** `site.yml` 是唯一认识「具体站点」的文件。

### 3.2 site.yml 各段做什么

| 段 | 关键字段 | 影响 |
|---|---|---|
| `site` | `name` / `base_url` / `extra_hosts` / `brand_aliases` | 抓取目标站；`extra_hosts` 决定「引用检测」判不算命中本站；`brand_aliases` 决定「提及检测」 |
| `publish` | `cms`（strapi / none）/ `base_url` / `token_env` / `content_type` | 发布目标和内容类型；`cms: none` = 只生成不发布 |
| `routes` | `article_prefix` / `locale_prefixes` / `static_paths` | 文章路由前缀（如 `/blog` → `/articles`）、语言前缀、静态页清单 |
| `categories` | `id` / `name` / `assignee` / `weight` / `style` | 内容分几类、谁负责、权重、写作风格。**增删品类只改这里** |
| `batch` | `categories_per_week` / `product_slot` / `product_category` | 每周批次构成 |
| `watermark` | `default` / `min_occurrences` / `overrides` | 品牌水印（质检门禁按它判定）。`overrides` 可按知识库条目换文案 |
| `quality` | `min_chars` / `min_h2` / `min_faq` / `min_tables` / `max_fix_rounds` | 质检阈值 |
| `citation_check` | `engine` / `cookie_env` / `queries` | 引用检测引擎、cookie 环境变量名、**查询词**（按站点定制） |
| `workflow.labels` | 各标签名 | 工单系统里的 label 名 |
| `people` | `default_assignee` / `assignees` | 负责人（会自动生成校验规则，写错人名会被拦） |
| `thresholds` | `sitemap_coverage_min` / `lcp_max_seconds` / `static_pages_expected` | 告警阈值 |

### 3.3 不同变更需要动什么

| 官网发生的变化 | 要改什么 | 要不要改代码 |
|---|---|---|
| 只换域名 | `site.base_url` + `site.extra_hosts` + `publish.base_url` | ❌ 不用 |
| 换品牌名/水印 | `site.name` + `brand_aliases` + `watermark.default` | ❌ 不用 |
| 加/删内容品类 | `categories` 增删条目 | ❌ 不用 |
| 换负责人 | `categories[].assignee` + `people.assignees` | ❌ 不用 |
| 博客路由改了 | `routes.article_prefix` | ❌ 不用 |
| 加语言 | `routes.locale_prefixes` 加一项 + `static_paths` | ❌ 不用 |
| 换知识库 | 换 `knowledge/` 目录 + `knowledge/index.yml` | ❌ 不用 |
| 换 CMS（不再是 Strapi） | 新写 `geoagent/clients/<新CMS>.py`，对齐 4 个方法（见 §6.3） | ✅ 要 |
| 换质检规则/阈值 | `quality` 段 | ❌ 不用 |

### 3.4 实测验证：真的能换

```bash
$ .venv/bin/python cli.py check --site site.example.yml
名称      : 示例科技
目标站    : https://www.example.com
品类      : product-news(产品动态/Alice) · tech-deep-dive(技术深度/Bob)
动态模型  : Category Literal: ['product-news', 'tech-deep-dive']

$ .venv/bin/python cli.py seo-daily --site site.example.yml
覆盖率      : None  (health=UNKNOWN)          ← 分母不可得 → 记 None 而不是 0
工单 assignee: Alice                          ← 来自配置，不是写死的
daily 里的站点名: 示例科技                     ← 来自配置
```

换站点的回归保护写进了测试：整套 63 项测试都跑在**虚构站点**
（`test.example.com` / 示例科技 / `product-news` / Alice）上 ——
一旦代码里残留任何真实站点硬编码，测试立刻失败。

---

## 四、换知识库怎么做

### 4.1 结构

```
knowledge/
  index.yml                  ★ 清单：谁被登记、覆盖哪些品类、有什么硬口径
  products/                  任意结构，由 index.yml 指向
  categories/                （推荐的下一步：品类事实卡）
```

### 4.2 index.yml 两种来源

| `knowledge.source` | 文件从哪读 | 用途 |
|---|---|---|
| `repo`（默认，有 git 副本时） | git 工作副本里的 `knowledge/` | 与现状一致：事实文件跟内容仓库放一起 |
| `local` | 本项目 `knowledge/` 目录 | 完全本地化/换知识库 |

> `index.yml` 按三级查找（`--knowledge` → `GEOAGENT_KNOWLEDGE` → `./knowledge/index.yml`
> → `~/.config/geoagent/knowledge/index.yml`），**可以放在仓库外**；
> 只有「事实文件」的根目录随 `source` 变。这样能「索引在仓库外、事实文件在内容仓库里」。

### 4.3 index.yml 一条登记长什么样

```yaml
entries:
  - id: example-widget               # 唯一标识（site.yml 的水印 override 按它匹配）
    title: 示例小部件 ExampleWidget
    files:
      facts: products/example-widget.md
      faqs: products/example-widget-faqs.md
    applies_to:
      categories: [product]          # 适用于哪些品类
      is_product_slot: true          # 是否占用每周「产品篇」名额
    watermark: ExampleWidget         # 本条目的水印
    angles: [产品总览, 会议纪要场景, ...]  # 产品篇角度轮换表（防同题重复）
```

`defaults` 段放全局硬口径（对所有条目生效）：

```yaml
defaults:
  policy: 事实必须来自登记文件，不得凭空编写参数/规格/名单
  rules:
    - 不写具体价格，统一表述为「硬件一次买断，无后续费用」
    - 竞品对比只做功能维度、中性措辞、不点名不贬损
```

### 4.4 关键行为：没有知识库的品类会怎样

**不会静默放任。** 撰写 prompt 里会明确写入：

```
⚠️ 本品类【无登记事实来源】：涉及具体参数/规格/名单/案例时，
只能用行业通用表述，不得编造具体数字或客户名。
```

有登记时则注入事实卡正文 + 硬口径 + 可选角度。

### 4.5 当前知识库覆盖情况（诚实说明）

| 主题 | 有无登记 | 风险 |
|---|---|---|
| 产品篇（当前唯一登记条目） | ✅ 有（2 个文件） | 低 |
| 其余各品类 | ❌ 无 | **中高** —— 事实全靠 LLM 自身知识 |

**建议（未实施）**：给每个品类建一份 `knowledge/categories/<品类>.md`，
在 index.yml 登记。这是提升内容可信度性价比最高的一步。

---

## 五、内容从哪来（四层来源）

| 层 | 来源 | 说明 |
|---|---|---|
| 1 选题 | `site.yml` 的 `categories` + `citation_check.queries` + `batch` | 品类、权重、产品篇角度轮换、按"用户会怎么问 AI"反推选题 |
| 2 事实 | `knowledge/index.yml` 登记的文件 | 产品篇强制对齐事实卡；无登记的品类明确禁止编造 |
| 3 规范 | `site.yml` 的 `quality` / `watermark` + 质检脚本 10 条门禁 | 字数/H2/FAQ/表格/水印/时效性/禁 blockquote 开篇/th-td 不同行混用 |
| 4 触发 | Gitea 工单（`workflow.labels` 定义标签名） | 每周批次 6 张（5 品类 + 1 产品篇）；管道 A/B 的异常工单 |

链路：选词 → 建工单 → LLM 撰写（注入知识库事实）→ 质检回炉 ≤3 轮 → `status: review`
→ 质检 + 转换 + 查重 → 发布 → 评论 ④ + 关单 + 回写。

---

## 六、写权限：三重锁 + 影子期规则

### 6.1 发到官网需要同时满足 3 个条件

```
① site.yml:    publish.enabled: true      ← 默认 false（用户 2026-09-18 要求：不要发布到官网）
② config.yml:  shadow.read_only: false    ← 默认 true
③ 命令行:      --apply
```

只要 ① 是 false，即使 ②③ 全放开也会被拒绝：

```
strapi_publish_blocked  method=POST path=/api/articles reason=publish.enabled=false
```

另外 `site.yml` 的 `publish.cms: none` 是第四重保险 —— 连 Strapi 客户端都不构造，用空发布器。

### 6.2 各写操作的放行条件

| 操作 | 条件 |
|---|---|
| 发布/更新文章到 CMS | ① + ② + ③ + `cms != none` |
| Gitea 建工单 / 评论 / 关闭 | ② + ③ |
| git commit / push | ② + ③ |
| 企业微信群消息 | `--notify`（**独立**；影子期也能发） |

### 6.3 换 CMS 的接口契约

新写一个类，实现这 4 个方法，然后在 `site.yml` 里 `publish.cms: <你的名字>`：

```python
articles_total()                      -> int | None        # 用于覆盖率分母
find_by_slug(slug)                    -> list[dict]        # 查重（含 documentId）
create_article(payload) / update_article(document_id, payload)
verify_slug_urls(slug)                -> {path: http_code} # 发布后验证
clean_fields(payload)                 -> dict              # 剥引号 + 品类白名单
```

参考实现：`geoagent/clients/strapi.py`（Strapi v5）与 `NoopCms`（不发布）。

---

## 七、字段与文件地图

```
geo-agent/
  config.example.yml                  运行配置【占位骨架】（地址用占位符，凭据写 ${ENV}）
  site.example.yml                    ★ 站点档案【虚构骨架】
  knowledge/index.example.yml         ★ 知识库清单【虚构骨架】
  ↑ 真实的 config.yml / site.yml / knowledge/index.yml 一律放仓库外（见 §7.1）
  requirements.txt                    依赖，全部 pin
  cli.py                              入口：5 条管道 + list + check
  README.md / 使用说明.md

  geoagent/
    config.py            运行配置加载（含凭据解析：${ENV} / 环境变量 / 字面值）
    site.py              ★ 站点档案加载与解释（hosts/brand/watermark/routes/labels/validate）
    knowledge.py         ★ 知识库索引（entries/for_category/facts/grounding_block）
    schema_loader.py     ★ 按站点动态生成校验模型（品类/负责人 Literal）
    obs.py               JSONL 结构化日志 + trace
    llm.py               LLM 工厂 + 结构化输出（原生不可用自动回退文本+JSON解析）
    schemas.py           站点无关的通用模型（WeekReview / ArticleDraft / FixInstruction）
    quality.py           质检门禁（官方脚本 + 站点水印覆盖）+ md→HTML
    repo.py              git（只 add 精确文件 / clone 修损坏 / 影子输出目录）
    clients/
      gitea.py           工单 CRUD
      strapi.py          发布器（Strapi + NoopCms + build() 工厂）
      wecom.py           群机器人（errcode 校验 / 限频合并 / 静默期）
    metrics/
      coverage.py        覆盖率纯函数（locale 去重 / null≠0 / 超索引伪影 / 路由前缀可配）
      normalize.py       4 种历史 schema + checks 包装层归一化
    nodes/               common / publish / seo / geo / strategy / content / notify
    graphs/              publish / seo_daily / geo_weekly / strategy / content
    tools/               上游脚本的定制副本（见 §9）

  tools_audit_no_write.py    只读取证：确认从未写入官网/Gitea
  tools_shadow_diff.py       影子对拍：新产物 vs 线上归档逐字段比
  tools_fetch_*.py           只读采集 fixture
  audit_hardcode.sh          扫描非通用硬编码

  tests/                     63 项 pytest，全部跑在虚构站点上
    fixtures/site.test.yml   ★ 虚构站点档案（测试专用）
    fixtures/site.other.yml  ★ 第二份虚构站点（验证换站点不崩）
    fixtures/metrics/        63 个真实 metrics 归档（归一化回归用）
  var/                       运行时产物（logs / out / workspaces / state）
```

### 7.1 私有档案放哪（仓库外）

`config.yml` / `site.yml` / `knowledge/index.yml` 都是**私有档案**，不进版本控制。
三者查找顺序一致：

```
① 命令行         --config / --site / --knowledge <绝对路径>
② 环境变量       GEOAGENT_CONFIG / GEOAGENT_SITE / GEOAGENT_KNOWLEDGE
③ 项目内         ./config.yml / ./site.yml / ./knowledge/index.yml   ← .gitignore 已忽略
④ 用户配置目录   ~/.config/geoagent/config.yml 等                    ← 推荐
```

分发口径：

| 放哪 | 好处 | 注意 |
|---|---|---|
| `~/.config/geoagent/`（推荐） | 多个 checkout / 多台机器共用一份，仓库怎么拉都不会带出私有信息 | cron 里建议写全绝对路径 |
| 项目内 `./site.yml` | 直接跑不用带参数 | 已被 .gitignore 忽略，别 `git add -f` |

- 目录可用 `GEOAGENT_HOME` 改（默认 `~/.config/geoagent`）。
- 仓库里只有三份 `.example.yml` 骨架：**零真实域名 / IP / 用户名 / 凭据**，只有结构。
- 凭据在 YAML 里一律写 `${ENV_NAME}`，真值放环境变量或 `.env`（`.env` 也在 .gitignore 里）。

---

## 八、日常运维

```bash
# 配置自检（改完 site.yml / knowledge/index.yml 先跑这个）
.venv/bin/python cli.py check

# 看一次运行发生了什么
ls -t var/logs/ | head
cat var/logs/latest-seo-daily.jsonl

# 确认「有没有偷偷写到生产」
.venv/bin/python tools_audit_no_write.py

# 影子对拍（新旧产物逐字段比）
.venv/bin/python tools_shadow_diff.py [--date 2026-09-18]

# 扫描非通用硬编码
bash audit_hardcode.sh

# 回归
.venv/bin/python -m pytest tests -q
```

**失败不静默**：任何节点失败都写 `state["errors"]`，会落 trace + 终端报告 + 通知告警。

---

## 九、通用化改造说明（v0.2）

### 9.1 做了什么

把 9 类、38 处硬编码收拢成三份配置：

| 原来硬编码在哪 | 现在从哪来 |
|---|---|
| 域名（14 处） | `site.base_url` / `extra_hosts` / `publish.base_url` |
| 品牌水印（8 处） | `watermark.default` + `overrides` + `knowledge` 条目的 `watermark` |
| 品类枚举（7 处） | `categories` → 动态生成 pydantic Literal |
| 负责人（12 处） | `people` / `categories[].assignee` → 动态生成 Literal |
| 知识库路径（2 处） | `knowledge/index.yml` 的 `files` |
| 博客路由（6 处） | `routes.article_prefix` / `locale_prefixes` |
| 静态页清单（1 处） | `routes.static_paths` |
| 引用查询词（7 条） | `citation_check.queries` |
| 凭据（3 处） | `token_env` / `cookie_env` / 环境变量 |

修掉了两处**会直接崩**的地方（品类/负责人 Literal 写死）和一处**会静默出错**的地方
（引用检测的域名白名单写死 —— 换域名后永远判"未引用"，会误建 P0 工单）。

### 9.2 关于 `geoagent/tools/` 里的上游脚本

3 个脚本是上游的**定制副本**，改动是「把阈值和品牌参数化」：

- `verify_article_quality.py`：`--watermark` / `--min-chars` / `--min-h2` … 可传
- `geo_citation_check.py`：`--cookie` / `--cookie-env` / `--queries-file` / `--site-hosts` /
  `--brand-aliases` / `--assignees` 可传（**cookie 不再硬编码在代码里**）
- `sitemap_coverage.py`：未参数化（geo-agent 不依赖它，用自己的 coverage.py）

**不传参数时行为与上游完全一致**（向后兼容）；遗留默认值集中在带
`# ---- LEGACY DEFAULTS ----` 标记的块里，测试会核实「除了这个块，代码里不许出现站点痕迹」。

### 9.3 通用性怎么证明的

1. **63 项测试全部跑在虚构站点上**（`test.example.com` / 示例科技 / `product-news` / Alice）
2. **专项断言**：`test_generality.py` 逐文件核实活代码里无站点痕迹；两份不同 site.yml
   必须产出不同的校验模型；同一篇文章在两个站点下水印判定结论不同
3. **实测换站点**：`cli.py seo-daily --site site.example.yml` 跑通（抓取全部 404 也优雅降级）
4. **对拍**：换配置后管道 A 产物与线上归档 4 项硬指标仍一致

---

## 十、已知限制与待办（未实施）

| # | 事项 | 说明 | 优先级 |
|---|---|---|---|
| 1 | 5 个品类没有事实知识库 | 建议建 `knowledge/categories/*.md` 并登记 | 中高 |
| 2 | 企业微信通知未实测 | webhook key 须在**企业微信里人工创建**；给我 key 即可用 `--notify` 打真消息 | 中 |
| 3 | 引用检测依赖 sessionid cookie（2-7 天过期） | 失效时走「不虚构数据」分支；需定期刷新（现在用 `DOUBAO_COOKIE` 环境变量，不再写进代码） | 中 |
| 4 | 未接入调度 | 目前全部手工触发；稳定后可加 cron 指向 `cli.py <管道>`（旧 4 个 job 先不动） | 低 |
| 5 | 未做 CMS 的 file/wordpress 实现 | 接口已留好（§6.3），需要时按契约实现 | 低 |
| 6 | `sitemap_coverage.py` 未参数化 | 该脚本是上游遗留，geo-agent 不用它 | 低 |
| 7 | LLM 节点 `temperature=0` 但输出非完全确定 | 对拍允许措辞差异，数字必须一致 | 已知 |

---

## 十一、与旧系统的关系

```
旧系统（保留、未动）                         新系统（本项目）
Hermes 4 个 cron job                          geo-agent/
  d97bf6c794f3 SEO每日      0 9 * * *          publish / seo-daily /
  13364927228f GEO每周      0 10 * * 1  ──▶    geo-weekly / strategy / content
  e08e737ff7d5 策略分析     0 8 * * 0
  033210b0e986 定时发布     0 10 * * 2,5        site.yml 一份档案描述一个站点
旧 skill（流程写在文档里，由 LLM 每次现读）      代码状态机 + 3 个 LLM 判断点
共享 clone /tmp/<原共享目录>                  独立 var/workspaces/<站点>
```

- 旧 cron **仍在执行**，仍会向官网发文（与本项目的发布逻辑并行）
- 新旧**影子并行 2 周**，逐日对拍通过后才谈摘旧 job
- 本项目已建独立仓库（`geo-agent`，私有）；站点身份与凭据只留在仓库外的私有档案里，
  仓库里只有零真实取值的 `.example.yml` 骨架
